/**
 * Shared constants for the reasoning feature.
 * Safe to import from both client and server — no Node.js or SDK dependencies.
 */

export const FALLBACK_REASONING =
  "This combination balances proportions and maintains a cohesive color palette suited to the occasion.";
